name: Deploy Daneshyar to PGLCP

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

concurrency:
  group: deploy-pglcp-daneshyar-bridge
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  verify-build-deploy:
    runs-on: [self-hosted, Windows, X64, pglcp-deploy]
    timeout-minutes: 45
    defaults:
      run:
        shell: bash
    env:
      DEPLOY_HOST: ${{ secrets.PGLCP_DEPLOY_HOST }}
      DEPLOY_PORT: ${{ secrets.PGLCP_DEPLOY_PORT }}
      DEPLOY_USER: ${{ secrets.PGLCP_DEPLOY_USER }}
    steps:
      - name: Check out Daneshyar
        uses: actions/checkout@v4
        with:
          repository: fariborzdehghani/enterprise-forms-knowledge-platform
          ref: main
          token: ${{ secrets.DANESHYAR_REPO_TOKEN }}
          persist-credentials: false

      - name: Configure deployment SSH
        env:
          DEPLOY_SSH_KEY: ${{ secrets.PGLCP_DEPLOY_SSH_KEY }}
          DEPLOY_KNOWN_HOSTS: ${{ secrets.PGLCP_KNOWN_HOSTS }}
        run: |
          mkdir -p ~/.ssh
          chmod 700 ~/.ssh
          printf '%s\n' "$DEPLOY_SSH_KEY" > ~/.ssh/pglcp_deploy_key
          printf '%s\n' "$DEPLOY_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/pglcp_deploy_key ~/.ssh/known_hosts

      - name: Check deployed revision
        run: |
          set -euo pipefail
          TARGET_SHA="$(git rev-parse HEAD)"
          DEPLOYED_SHA="$(ssh -i ~/.ssh/pglcp_deploy_key -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" 'cat /opt/apps/daneshyar/deployed-sha 2>/dev/null || true')"
          echo "TARGET_SHA=$TARGET_SHA" >> "$GITHUB_ENV"
          if [ "$TARGET_SHA" = "$DEPLOYED_SHA" ] && [ "${{ github.event_name }}" != "workflow_dispatch" ]; then
            echo "DEPLOY_NEEDED=false" >> "$GITHUB_ENV"
            echo "Daneshyar revision $TARGET_SHA is already deployed."
          else
            echo "DEPLOY_NEEDED=true" >> "$GITHUB_ENV"
          fi

      - name: Set up pnpm
        if: env.DEPLOY_NEEDED == 'true'
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        if: env.DEPLOY_NEEDED == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        if: env.DEPLOY_NEEDED == 'true'
        run: pnpm install --frozen-lockfile

      - name: Verify application
        if: env.DEPLOY_NEEDED == 'true'
        run: |
          pnpm exec next typegen
          pnpm test
          pnpm typecheck
          pnpm lint

      - name: Build deployment image
        if: env.DEPLOY_NEEDED == 'true'
        run: docker build --tag "pglcp/daneshyar:$TARGET_SHA" .

      - name: Transfer image and deploy
        if: env.DEPLOY_NEEDED == 'true'
        run: |
          set -euo pipefail
          IMAGE_REF="pglcp/daneshyar:$TARGET_SHA"
          docker save "$IMAGE_REF" | gzip -1 | ssh -i ~/.ssh/pglcp_deploy_key -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" 'gunzip | docker load >/dev/null'
          scp -i ~/.ssh/pglcp_deploy_key -P "$DEPLOY_PORT" deploy/compose.production.yml "$DEPLOY_USER@$DEPLOY_HOST:/opt/apps/daneshyar/compose.yml"
          ssh -i ~/.ssh/pglcp_deploy_key -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" "if systemctl is-active --quiet daneshyar.service; then systemctl stop daneshyar.service; fi; cd /opt/apps/daneshyar && DANESHYAR_IMAGE='$IMAGE_REF' docker compose -p daneshyar -f compose.yml up -d --force-recreate --remove-orphans"
          ssh -i ~/.ssh/pglcp_deploy_key -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" "export DANESHYAR_IMAGE='$IMAGE_REF'; for attempt in \$(seq 1 30); do curl -fsS http://127.0.0.1:3201/api/health >/dev/null && exit 0; sleep 3; done; docker compose -p daneshyar -f /opt/apps/daneshyar/compose.yml logs --tail=120; docker compose -p daneshyar -f /opt/apps/daneshyar/compose.yml down; if [ -f /etc/systemd/system/daneshyar.service ]; then systemctl start daneshyar.service; fi; exit 1"
          ssh -i ~/.ssh/pglcp_deploy_key -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" "printf '%s\n' '$TARGET_SHA' > /opt/apps/daneshyar/deployed-sha"
          ssh -i ~/.ssh/pglcp_deploy_key -p "$DEPLOY_PORT" "$DEPLOY_USER@$DEPLOY_HOST" 'docker image prune -f >/dev/null'
